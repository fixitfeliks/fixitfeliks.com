class ImageGrid {
    constructor(rows, cols) {
        this.rows = rows;
        this.cols = cols;
    }
}

class ImageTile {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
}
